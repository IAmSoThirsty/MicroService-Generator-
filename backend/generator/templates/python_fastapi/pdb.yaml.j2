{% if config.kubernetes.enable_pdb -%}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ service_name }}
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: {{ service_name }}
{% endif -%}
