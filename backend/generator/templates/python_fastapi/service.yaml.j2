apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}
  namespace: default
  labels:
    app: {{ service_name }}
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ service_name }}
  sessionAffinity: None
